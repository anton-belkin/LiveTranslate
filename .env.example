# Server-side only. Do NOT put secrets in the browser.

# Azure Speech (server-side STT + MT via TranslationRecognizer)
AZURE_SPEECH_KEY=your_key_here
AZURE_SPEECH_REGION=your_region_here
# Optional: custom endpoint if your Azure resource requires it.
AZURE_SPEECH_ENDPOINT=
# Optional: auto-detect source languages (comma-separated).
AZURE_SPEECH_AUTO_DETECT_LANGS=de-DE,en-US,ru-RU
# Optional: translation targets (comma-separated).
AZURE_SPEECH_TRANSLATION_TARGETS=de,en,ru
# Optional: diarization (speaker IDs) if supported by Azure SDK.
AZURE_SPEECH_DIARIZATION=false
# Optional: target sample rate for Azure ingestion (Hz). Default 16000.
AZURE_SPEECH_SAMPLE_RATE_HZ=16000

# ws://localhost:8787 by default in this PoC
PUBLIC_WS_URL=ws://localhost:8787

# Web app (Vite) default WS URL
VITE_WS_URL=ws://localhost:8787
